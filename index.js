// index.js
const { generateRandomNumber, generateRandomArray, calculateArrayAverage } = require('./random');

module.exports = {
    generateRandomNumber,
    generateRandomArray,
    calculateArrayAverage
};
